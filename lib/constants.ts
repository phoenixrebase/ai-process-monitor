export const CANDIDATE_LABELS = ["complies", "deviates", "unclear"] as const;

export const API_TIMEOUT = 30_000;
export const CLIENT_TIMEOUT = 600_000;

export const DEFAULT_RETRIES = 2;
export const NO_RETRIES = 0;

export const DEFAULT_RESULTS_LIMIT = 100;
export const DEFAULT_ITEMS_PER_PAGE = 15;
